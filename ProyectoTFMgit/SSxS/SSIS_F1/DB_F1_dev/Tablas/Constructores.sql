CREATE TABLE [dbo].[Constructores]
(
	[constructorId] [int] NOT NULL PRIMARY KEY,
	[constructorRef] [varchar](255) NULL,
	[name] [varchar](255) NULL,
	[nationality] [varchar](255) NULL,
	[url] [varchar](255) NULL

)
